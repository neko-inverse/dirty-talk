# 测试报告

日期：2026-09-12。运行环境：Windows、Node.js 24.15.0、本机 Google Chrome（独立无头测试配置）、Playwright 1.63.0。

## 最终结果

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 通过：服务端、表达引擎、浏览器脚本语法 |
| `npm test` | 17 / 17 通过 |
| `npm run test:browser` | 24 / 24 通过，10.3 秒（中文默认语言改版） |
| 依赖安装审计 | 0 个已报告漏洞 |
| 独立只读代码复核 | 已处理请求取消传播、密钥残留、弹窗命名、错误中文化及配置校验问题 |
| 桌面与手机视觉检查 | 已查看真实浏览器截图，双栏工作台、上下布局和模型配置可用 |

自动测试合计 41 项；其中示例测试遍历 4 场景 × 3 风格 × 2 语言的全部 24 个组合。「Dirty → 得体」改版覆盖风格/语言自动联动、快速切换、未知输入的参考标注、已配置模型时不自动发送密钥。国内模型目录新增 8 家厂商全部预设 ID、密钥链接、当前主推说明与服务地址的一一对应验证，以及旧海外配置迁移和官方模型参数隔离验证。修复根域接口拼接双斜线并加入回归断言。新版 320px 与 1440px 模型设置截图已人工查看。

## 覆盖范围

- 两张参考图的原句、预设样例及别名匹配，中英语言与三种风格。
- 空输入、无效参数、2,000 字符边界、未知文本明确失败、不依据关键词编造结果。
- 静态资源、健康检查、HEAD、404、服务端源码与原图不对外提供。
- JSON、请求大小、跨站来源限制，HTTPS 与密钥校验、内网地址阻断。
- AI 参数转发、固定连接测试文本、安全错误提示与异常处理。
- 用户取消时，服务端模型请求也收到取消信号。
- 所有服务商预设与自定义模型，密钥显示/隐藏、切换服务商清空密钥。
- 保存偏好但不保存密钥，刷新后恢复偏好、回到示例模式。
- 连接测试成功、修改配置后连接状态失效、保存并进入 AI 模式。
- 原生弹窗 Esc 关闭与焦点恢复。
- 清空、字数、快捷键、复制到剪贴板。
- 网络失败、非 JSON 返回、上游授权错误，以及旧响应不覆盖新输入。
- 模型返回的 HTML 当作普通文本显示，不执行。
- 320、390、768、1440 像素页面和配置弹窗无横向溢出，并生成截图。
- 可选 WebMCP 的注册结构、有效与无效输入，以及更新同一可见页面状态。

## 验证边界

- 未提供真实 API Key，因此各供应商的真实付费推理、账户权限、额度和实际文案质量尚未验证。AI 成功/失败的前端流程使用受控模拟响应；服务端参数传递使用注入的测试替身，不伪装成供应商实测。
- WebMCP 在模拟注册上下文验证了契约，未在支持该提案的真实宿主验证，不声称已验证宿主兼容性。
- 朗读依赖浏览器与操作系统语言包；未对实际声音输出进行听觉验收。
- 检查了命名、焦点、响应式布局并修正多处文字/焦点对比度，但未进行完整 WCAG 审计或所有屏幕阅读器测试。
- 当前连接的浏览器控制扩展不可用，改用项目内 Playwright 的独立无头 Chrome 执行自动化测试与截图；未访问用户浏览器配置或个人页面。

## 可复现

运行 `npm run check`、`npm test`、`npm run test:browser`。详细浏览器报告位于 `playwright-report/index.html`，截图位于 `test-results/`，这些是可重新生成的本地文件。

首轮浏览器测试的两项失败来自测试期望写错（标题不应包含 aria-hidden 装饰字符、示例实际为 9 字符）；修正后完整重跑 19 项全部通过。最终无跳过或失败用例。
